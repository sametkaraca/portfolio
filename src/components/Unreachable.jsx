import React from 'react';
import style from "../styles/main.module.scss";

export const Unreachable = () => {
    return (<section className={style.unreachable}>
        <h1>404</h1>
    </section>);
}